# 模块目录说明

当前阶段已完成：

- `health`：系统健康检查
- `auth`：管理员登录、登出、当前登录态查询
- `user`：管理员初始化、当前账户密码修改
- `post`：管理员侧文章创建、列表、详情、更新基础接口

后续计划模块：

- `post`：文章 CRUD 与发布流转
- `category`：分类管理
- `tag`：标签管理
- `comment`：评论提交与审核
- `setting`：站点设置
- `search`：搜索能力