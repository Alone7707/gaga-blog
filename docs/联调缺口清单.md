# 联调缺口清单

## 本次检查范围

- 文档：`docs/` 下现有需求拆解、进度、UI 改版相关文档
- 前端：公开站点页面、后台仪表盘 / 文章 / 评论 / 设置主链路
- 后端：公开文章 / 分类 / 标签 / 搜索 / 评论 / 归档接口，以及后台文章 / 评论 / 设置 / 仪表盘接口

## 本轮新增完成项

### 1. 后台文章独立发布动作链路已接通

- **位置**：
  - 前端列表页：`frontend/src/pages/admin/AdminPostsPage.vue`
  - 前端编辑页：`frontend/src/pages/admin/AdminPostEditorPage.vue`
  - 前端 API：`frontend/src/api/posts.ts`
  - 类型：`frontend/src/types/post.ts`
- **修复内容**：
  - 补齐前端文章动作 API：
    - `PATCH /api/admin/posts/:id/publish`
    - `PATCH /api/admin/posts/:id/unpublish`
    - `PATCH /api/admin/posts/:id/archive`
  - 后台文章管理页新增独立动作按钮：发布 / 下线 / 归档。
  - 后台文章编辑页将“状态切换”从表单保存中拆出，改为独立动作区调用真实动作接口。
  - 编辑页发布前会先保存当前表单内容，再进入后端发布校验与发布时间处理链路。
- **收益**：
  - 前端状态流转和后端接口契约对齐，不再依赖“保存时偷偷改 status”。
  - 后端 `publish` 的单独校验、`publishedAt` 处理与冲突提示可以被真实覆盖。
  - 尽量只动文章页和文章 API，避免和 UI agent 产生大面积合并冲突。

### 2. 后台文章列表 / 编辑页补齐最小交互反馈与空值保护

- **位置**：
  - `frontend/src/pages/admin/AdminPostsPage.vue`
  - `frontend/src/pages/admin/AdminPostEditorPage.vue`
  - `frontend/src/api/posts.ts`
- **修复内容**：
  - 列表页新增动作成功提示、动作中 loading 锁定、错误提示复用。
  - 列表接口增加 `list / pagination` 兜底，避免异常包体导致管理页空白。
  - 编辑页分类 / 标签回填增加 `Array.isArray` 与空字段保护。
  - 编辑页新建成功后跳转到编辑页，避免还未拿到 `id` 就尝试执行发布动作。
  - 编辑页当前状态展示改为只读说明，避免继续误导为“保存表单即可完成发布”。
- **收益**：
  - 管理端文章链路在异常回包下更稳，联调时更容易定位是动作失败还是数据为空。
  - 发布动作可在列表页和编辑页两条链路上直接验证。

## 已完成的小修

### 1. 修复分类 / 标签总览页非法 slug 半空态

- **问题**：`CategoriesPage`、`TagsPage` 仅在 query 为空时回退首项，query 带非法 slug 时，高亮卡片区不会展示。
- **影响**：页面不会直接崩溃，但会出现“列表区正常、入口区空白”的半空态，清爽版 UI 下更明显。
- **修复**：前端统一增加 `normalizedActiveSlug`，当 query 缺失或非法时，自动回退到首个有效分类 / 标签。

### 2. 补上分类 / 标签文章页侧栏失败提示

- **问题**：`CategoryPostsPage`、`TagPostsPage` 的侧栏入口请求失败时仅结束 loading，没有任何提示。
- **影响**：主内容区可能正常，但左侧导航静默空白，联调时难以判断是无数据还是接口失败。
- **修复**：补充 `sidebarErrorMessage`，在侧栏区域明确展示“分类入口加载失败 / 标签入口加载失败”。

### 3. 补强后台评论邮箱空值兜底

- **问题**：后台评论页类型定义和 `maskEmail` 默认假设 `authorEmail` 一定是字符串。
- **影响**：若后端返回 `null`（例如匿名评论或后续契约放宽），评论列表与详情区都可能出现运行时异常。
- **修复**：前端类型改为 `string | null`，并在邮箱脱敏函数中增加空值兜底，统一显示 `--`。

### 4. 修复公开文章详情接口包裹层不一致

- **问题**：后端 `GET /api/public/posts/:slug` 返回结构为 `{ post: ... }`，但前端 `getPublicPostDetail` 直接按详情对象消费。
- **影响**：文章详情页 `PostDetailPage` 直接拿到包裹对象时，`title`、`summary`、`tags`、`contentMarkdown` 等字段会变成 `undefined`，页面容易整块空白或局部渲染异常。
- **修复**：前端 API 层统一解包，改为 `.then((response) => response.post)`。

### 5. 补强公开评论列表的空值保护

- **问题**：`PostDetailPage` 直接把 `response.list` 赋值给 `comments`。
- **影响**：若接口未来短暂返回异常结构、代理层丢字段，评论区 `reduce / length / replies` 相关渲染存在崩溃风险。
- **修复**：增加 `Array.isArray(response.list) ? response.list : []` 兜底，并进一步前移到 `frontend/src/api/public.ts` 统一处理。

## 复查结论

### 一、公开主链路复查结果

#### 1. About 主链路

- **状态**：已接通
- **位置**：
  - 前端页面：`frontend/src/pages/public/AboutPage.vue`
  - 路由：`frontend/src/router/index.ts`
  - 接口：`frontend/src/api/site.ts`
  - 后端：`backend/src/modules/setting/public-site.controller.ts`
- **结论**：`/about` 已注册，页面已消费 `GET /api/public/site/pages/about` 与 `GET /api/public/site/overview`，此前文档中“关于页未接通”的结论已过期。

#### 2. Site overview / 首页 / 公共布局主链路

- **状态**：已接通
- **位置**：
  - 首页：`frontend/src/pages/public/HomePage.vue`
  - 公共布局：`frontend/src/layouts/PublicLayout.vue`
  - 接口：`frontend/src/api/site.ts`
- **结论**：站点标题、副标题、描述、页脚、favicon、About 摘要入口均已消费公开站点接口；本轮补的是 API 层容错，不是重新接线。

#### 3. Comments 主链路

- **状态**：已接通并补强兜底
- **位置**：
  - 前端：`frontend/src/pages/public/PostDetailPage.vue`
  - 接口：`frontend/src/api/public.ts`
  - 后端：`backend/src/modules/public-comment/public-comment.controller.ts`
- **结论**：评论列表 / 回复 / 提交评论链路已完整；本轮主要补结构容错，避免异常回包时前台讨论区失稳。

#### 4. Archives 主链路

- **状态**：已接通并补强兜底
- **位置**：
  - 前端：`frontend/src/pages/public/ArchivesPage.vue`
  - 接口：`frontend/src/api/public.ts`
  - 后端：`backend/src/modules/public-archive/public-archive.service.ts`
- **结论**：归档页当前能稳定消费接口；剩余问题不是“页面没接通”，而是“暂未分页”。

#### 5. Search 主链路

- **状态**：已接通并补强兜底
- **位置**：
  - 前端：`frontend/src/pages/public/SearchPage.vue`
  - 接口：`frontend/src/api/public.ts`
  - 后端：`backend/src/modules/public-search/public-search.service.ts`
- **结论**：搜索页已消费公开搜索接口；本轮补了 `keyword/list/pagination` 的兜底，降低异常结构影响。

### 二、前后端字段 / 包裹结构

#### 1. 公开文章详情包裹层不一致

- **状态**：已修
- **位置**：
  - 后端：`backend/src/modules/public-post/public-post.controller.ts`
  - 前端：`frontend/src/api/public.ts`

#### 2. 后台文章发布动作契约已对齐

目前后台文章状态流转已明确分为两类请求：

- 内容保存：`POST /api/admin/posts`、`PATCH /api/admin/posts/:id`
- 独立动作：`PATCH /api/admin/posts/:id/publish|unpublish|archive`

**结论**：

- 编辑页与列表页都已切到独立动作接口。
- 编辑保存不再混入 `status`，避免和后端发布动作语义冲突。
- 发布前先保存内容，再调用发布动作，符合后端现有契约。

#### 3. 后台接口风格仍不完全统一

目前仍存在两类返回方式：

- 直接返回对象：如 `GET /api/admin/posts`、`GET /api/admin/settings`、`GET /api/admin/dashboard/overview`
- 带业务键包裹：如 `GET /api/admin/posts/:id -> { post }`、`GET /api/admin/comments/:id -> { comment }`、`GET /api/admin/comments/stats -> { stats }`

**风险**：

- 后续新页面如果继续按“想当然的统一风格”接接口，仍可能出现解包错误。
- 本轮已把文章列表接口做了最小兜底，但后台管理端整体还没有统一封装规范。

**建议**：

- 后续补一份 `docs/API返回约定.md`，明确“列表接口 / 详情接口 / 动作接口”的 envelope 规范。

### 三、仍然存在的关键缺口

#### 1. 评论审核页只有审核动作，没有后台回复能力

- **现状**：
  - 前台支持提交评论和回复评论；
  - 后台支持审核评论、看回复链；
  - 但后台没有“管理员直接回复评论”的能力。
- **影响**：若后续产品希望管理员在后台直接运营互动，目前链路是断的。
- **建议优先级：中**
  - 若当前版本只做审核，可保留；
  - 若 UI 改版中准备突出评论运营，则需要新增接口与页面交互。

#### 2. 归档页接口已完成，但与设置项 `content.archivePageSize` 尚未打通

- **位置**：
  - 后端：`backend/src/modules/public-archive/public-archive.service.ts`
  - 设置：`content.archivePageSize`
- **现状**：归档接口当前一次性返回全部公开文章并按年月分桶；设置里的归档分页大小暂未参与公开归档接口。
- **影响**：
  - 数据量上来后，前端首屏渲染成本会增加；
  - 设置项目前更偏“预留字段”，没有真正落到公开链路。
- **建议优先级：中**
  - 短期可继续保留；
  - 若 UI 改版要重做归档页，建议一并决定是否需要分页 / 折叠加载。

### 四、工程与环境阻塞

#### 1. 当前仓库已可执行基础 build 校验

- **现状**：本轮复查发现 `npm.cmd` 可直接使用，不再是“完全无法构建”的状态。
- **处理建议**：前端、后端均应在本轮改动后至少各跑一次 build，作为联调收口基线。

## 建议的后续联调顺序

1. **先补后台 API 返回约定文档**：避免新 UI 再踩解包错误。
2. **继续补评论后台回复能力**：当前审核链路有了，但运营链路仍断。
3. **决定归档分页策略**：是否让 `content.archivePageSize` 真正生效。
4. **环境补 build / 冒烟验证**：至少保证 UI 改版前基线可编译，并验证文章动作主链路。

## 本轮结论

- 公开主链路中，about、site overview、comments、archives、search **都已接通**。
- 本轮已完成后台文章 **独立发布动作链路收口**，列表页与编辑页都可直接触发 publish / unpublish / archive。
- 当前剩余阻塞主要集中在：**后台评论回复能力缺失、归档分页策略未定、后台接口返回规范尚未文档化**。
